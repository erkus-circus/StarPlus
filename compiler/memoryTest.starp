func main@int() {
    return pow(2,5);

}

func stringToInt@int (str@string) {

    var size@int = size(str);

    var res@int = 0;
    while size > 0 {
        var temp@int = size;
        var power@int = 0;
        res = res + size;

        size = size - 1;
    }

    return res;
}

func pow@int (base@int, power@int) {
    var res@int = base;
    power = power - 1;
    while power >= 0 {
        res = res * res;
        power = power - 1;
    }
    return res;
}